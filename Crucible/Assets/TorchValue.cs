using UnityEngine;
using System.Collections;

public class TorchValue : MonoBehaviour {

	//Hold the value for the torch light.
	//Squares light up if in range of a character with this script attached.

	public float torchRange = 1.5f;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
